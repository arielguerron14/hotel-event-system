{
  "name": "hotel-event-system",
  "version": "1.0.0",
  "description": "Sistema de gestión de eventos para una cadena de hoteles.",
  "scripts": {
    "start:all": "docker-compose -f docker/docker-compose.dev.yml up --build",
    "stop:all": "docker-compose -f docker/docker-compose.dev.yml down",
    "lint": "eslint .",
    "test": "jest --config jest.config.js",
    "format": "prettier --write .",
    "build": "docker-compose -f docker/docker-compose.prod.yml build",
    "deploy": "docker-compose -f docker/docker-compose.prod.yml up -d"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "winston": "^3.8.2"
  },
  "devDependencies": {
    "eslint": "^8.40.0",
    "eslint-config-prettier": "^8.8.0",
    "jest": "^29.0.0",
    "prettier": "^2.9.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/hotel-event-system.git"
  },
  "author": "Tu Nombre",
  "license": "MIT"
}
